name = "orange-meets"

account_id = "7829299e6cefc232ded8635f50b53462"
main = "build/index.mjs"
# https://developers.cloudflare.com/workers/platform/compatibility-dates
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[[durable_objects.bindings]]
name = "rooms"
class_name = "ChatRoom"

[vars]
CALLS_APP_ID = "f3324a25c97d9902cff9018a0efcf3b1"
CALLS_API_URL = "https://rtc.live.cloudflare.com/v1"
TURN_SERVICE_ID = "3dba939d4fa9d8bf17639482bc2c2c61"

EXPERIMENTAL_SIMULCAST_ENABLED = "true"
BACKGROUND_IMAGE_URL = "https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=2564&auto=format&fit=crop"

MAX_WEBCAM_BITRATE = 8500000
MAX_WEBCAM_FRAMERATE = 60
MAX_WEBCAM_QUALITY_LEVEL = 1080

# ====== 云端CC（语音识别） ======
# 发送音频到服务端识别，比浏览器本地识别更准确
ENABLE_WORKERS_AI_ASR = "true"  # 启用云端CC
WORKERS_AI_ASR_MODEL = "@cf/deepgram/nova-3"  # Deepgram语音识别模型

# ====== 翻译功能（二选一） ======
# 作用：将字幕实时翻译成其他语言
# 目标语言：下面 WORKERS_AI_TRANSLATION_TARGET_LANGS 配置

# 方式1: OpenAI 翻译（推荐，质量更好）
USE_OPENAI_TRANSLATION = "true"
OPENAI_TRANSLATION_MODEL = "gpt-4.1-nano-2025-04-14"  # 或 gpt-4o, gpt-3.5-turbo

# 方式2: Workers AI 翻译（Cloudflare原生，免费但质量一般）
ENABLE_WORKERS_AI = "false"  # 翻译开关
WORKERS_AI_TRANSLATION_MODEL = "@cf/meta/m2m100-1.2b"  # 留空则不翻译

# 翻译目标语言（逗号分隔）
# 示例："en,zh" 会将字幕翻译成英文和中文两个版本
WORKERS_AI_TRANSLATION_TARGET_LANGS = "en,zh"  # 可选：ja,ko,es,fr,de等

# ====== AI 助手（可选） ======
OPENAI_MODEL_ID = ""

[[d1_databases]]
binding = "DB" # i.e. available in your Worker on env.DB
database_name = "orange-meets-db"
database_id = "722818b7-63af-4395-bad1-c68ce86d2bd4"

[ai]
binding = "AI"

# Durable Object migrations
[[migrations]]
tag = "v1" # Should be unique for each entry
new_classes = ["ChatRoom"]

[observability]
enabled = true

# The necessary secrets are:
# - CALLS_APP_SECRET
# - OPENAI_API_TOKEN (if using OpenAI realtime AI or OpenAI Translation)
# Run `echo <VALUE> | wrangler secret put <NAME>` for each of these

# Optional vars/secrets for OpenAI realtime AI:
# OPENAI_MODEL_ENDPOINT = ""


